#include <stdio.h>

int main(void)
{
    // Using a while loop for repetition
    int i = 0;
    while (i < 3)
    {
        printf("meow\n");
        i++;
    }

    // Using a for loop for repetition
    for (int i = 0; i < 3; i++)
    {
        printf("meow\n");
    }
}